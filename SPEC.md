# Dow3 资金-资产匹配系统 · 规格说明（SPEC）

> Web3 Fund-to-Asset Operations — MVP Demo
> 在线：https://jadeel00.github.io/dow3-demo-V1.0/ ｜ 仓库：https://github.com/Jadeel00/dow3-demo-V1.0
> 技术栈：纯前端静态应用（原生 HTML / CSS / JavaScript，单一全局 `state`，刷新即重置，无后端）

---

## 一、系统定位

面向 EXF 规模化扩张场景，把**资产端（借款方需求）**与**资金端（资方供给）**做规则化、自动化撮合，替代人工手搓匹配。核心是一套标准化流程：

> 资产台账 → 资产方资质审核 → 资金方准入 → 资金到账 → 智能匹配建议 → 撮合执行 → 催办闭环 → 合规留痕

---

## 二、整体工作流与「门禁」关系

一笔资产要完成撮合，必须依次通过 **三大门、五道校验**：

```
[资产录入] ──> ① 资产方资质门（第三道门）
                  实控人 / 企业 / 店铺 三维度审核，需「已过审」
                       │ 通过
                       ▼
            ② 资金方准入门（5 条规则全过）
                  1 资产类型 ∈ 资金方可配类型
                  2 资产收益率 ≥ 资金方最低收益要求
                  3 配资金额 ∈ 资金方单笔额度范围
                  4 资产期限 ∈ 资金方期限偏好
                  5 资金窗口：起息日 ≤ 资产最晚需求日 且 窗口时长 ≥ 资产期限
                       │ 通过
                       ▼
            ③ 资金到账门（第二道门）
                  资金方名下须有「法币已到账」的流水可用
                       │ 通过
                       ▼
                  [生成撮合记录]
```

并行的**时间门（第四道）**贯穿始终：资产有「最晚需求日」倒计时，**过期即失效**，不再进入撮合。

> 门禁口诀：**资质先行 → 准入校验（含时间窗口）→ 资金到账 → 方可撮合**；任一不过，撮合被拦截并给出具体原因。

---

## 三、模块功能说明

### 0. 导航结构
- **核心业务**：工作台 / 资产管理 / 资金管理 / 资金-资产匹配
- **运营后台**：财务与磨损 / 合规与披露

### 1. 工作台（Dashboard）
- **5 张看板卡**（可点击跳转）：总到账金额、已匹配资金、总资产、已匹配资产、待匹配资产。
- **重点事项**：扫描全系统自动生成分级待办，每条带「级别（紧急/重要/提醒）+ 类别（资产/资金/合规/匹配）」标签，点击跳转对应模块；右上角可按级别筛选。
  - 紧急：资产临近/超过截止日
  - 重要：资质待审、资料缺失需催办、流水未到账
  - 提醒：已具备条件、可立即撮合的机会
- **操作流水**：所有关键动作留痕。

### 2. 资产管理（Assets）
- 资产卡片：类别（LOC / DPL / Fast Pay）、本金、收益率、缺口、**资质徽标**、**最晚需求日倒计时**。
- 资产详情：完整字段 + **资产方资质三维度审核**（实控人 / 企业 / 店铺，含关键指标）+ 已配资明细 + 匹配建议。
- **资料缺失**可一键向资产方发起催办（Demo 内模拟）。
- **查看审核材料**：跳转合规模块查看材料（合同管理系统对接后深链至专属材料包）。

### 3. 资金管理（Funding，Tab 切换）
- **资金方 Tab**：资金方台账（如 Lista / Volo / 蚂蚁数科），每家含准入画像（可配类型、最低收益、单笔额度、期限偏好）、**资金窗口（起息日→还款日）**、承诺/已部署/可用额度进度。支持**新增 / 编辑 / 停用·启用**；停用即退出撮合引擎。点开看名下流水与已配资产明细。
- **资金流转 Tab**：稳定币→法币六态状态机流水（稳定币待打款→已到账→换汇中→法币已形成→待提现→法币已到账），可**按状态筛选**、逐笔推进状态；流水绑定所属资金方；到账时自动提示可撮合的资产。

### 4. 资金-资产匹配（Matching）
- 指标：可用法币、待配缺口、匹配笔数、资产覆盖率。
- **撮合进度（门禁可视化）**：选定资产后，以顺序流程图实时展示四关进度——① 资产资质 → ② 时间窗口 → ③ 资金方准入 → ④ 资金到账。已通过亮绿✓、首个卡点标红✕、其后置灰「待检」，末尾给出结论（可撮合 / 暂不可撮合 · 已过 X/4 · 卡点），评审可一眼看懂某笔卡在哪一关。
- **新建匹配**：选资产→自动填入推荐金额→实时跑 5 条准入校验，逐家显示 ✅通过 / ⚠️通过但无可用法币 / ❌不符合（附原因），并在可立即配的资金方中标 ⭐推荐（按利差排序）。
- **智能匹配建议**：按「利差 = 资产收益率 − 资金方最低要求」推荐最优资金方，可行额度感知（支持部分配资）。
- **一键采纳推荐**：自动按利差拆分多家分笔方案，预览（可立即撮合 / 待资金到账 / 仍缺口）后确认，仅对已到账法币部分生成撮合。
- 匹配记录可解除。

### 5. 财务与磨损（Finance，运营后台）
- 稳定币兑换计算器（手续费、汇率磨损、形成法币）、成本拆账表。

### 6. 合规与披露（Disclosure，运营后台）
- **合规总看板**：合同覆盖率、资料完整度、待处理缺口、已催办等指标。
- 一键提取合规摘要；预留**跳转外部合同管理系统**入口（该系统由另一团队开发对接中）。
- 资方合同 / 资产形成资料状态清单，未完成项可发起催办。
- 接收来自资产详情的**资质材料上下文**。

---

## 四、核心数据模型（state）

| 实体 | 关键字段 |
|---|---|
| **asset 资产** | id、name、category、amount、yield、tenure、**deadline 最晚需求日**、status、docs、applicant、**qualification（实控人/企业/店铺三维度）** |
| **funder 资金方** | id、name、type、eligible 可配类型、minYield、minTicket/maxTicket、tenure 期限偏好、committed 承诺额度、**startDate/dueDate 资金窗口**、status 活跃/停用 |
| **fund 资金流水** | id、funderId、coin、amount、network、status 六态、fiat 形成法币 |
| **match 撮合** | id、assetId、fundId、funderId、amount、time |

派生函数：`assetRemaining`（缺口）、`qualOverall`（资质结论）、`daysLeft/urgency`（倒计时）、`funderAvailable`（可用额度）、`funderLiquidity`（已到账可用法币）、`checkEligibility`（5 条准入）、`recommendFunders`（利差推荐）、`buildSplitPlan`（分笔方案）。

---

## 五、关键业务规则速查

- **利差** = 资产收益率 − 资金方最低收益要求；越大越优，是推荐排序依据。
- **可行额度** = min(资产缺口, 资金方单笔上限, 资金方可用额度)；支持部分配资。
- **资产资质** 三维度全「通过」才「已过审」，任一「不通过」→「未过审」，否则「待审」。
- **倒计时**：≤3 天紧急、≤7 天临近、已过期失效。
- **两类时间**：资产侧「最晚需求日」（deadline）；资金侧「起息日→还款日」窗口；二者通过第五道门对齐。

---

## 六、范围与边界

- 本仓库仅维护 **Demo A：资金-资产匹配系统**（`index.html` / `dow3-demo.html` / `dow3-demo.js` / `dow3-demo.css`）。
- 全部为**虚构示例数据**，不涉及真实客户/资金，不接入真实支付或链上操作；催办、提取摘要、跳转外部系统均为站内模拟。
- 合同管理由另一套系统负责，本系统合规模块定位为「总看板 + 跳转入口」。
